# Implementation Plans

Generated by the improve skill on 2026-07-05 at commit `d333a73`. This is
a **fresh slate**: the previous plan set (001-023, all but one DONE) was
retired at the maintainer's request and its still-relevant knowledge is
carried in the "settled decisions" section below. Numbering restarts at
001.

Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

Work directly on `main` for these plans. Do not create or switch to a
plan branch, and do not commit, push, or open a PR unless the operator
explicitly instructs you to do so.

Context every executor needs: this repo is a **cookiecutter template**.
The project code lives under the literal directory
`{{cookiecutter.project_slug}}/` (quote it in shell), files inside it
contain Jinja placeholders that must stay valid, and verification always
means **baking** a project (`uvx cookiecutter . --no-input -o <dir>`)
and running the baked suite (`uv run pytest` → 100% coverage required;
`git add -A && uv run pre-commit run --all-files`) plus root
`pre-commit run --all-files`. `.github/workflows/*` and `.agents/*`
inside the template are copied WITHOUT rendering — never put Jinja or
slug-derived values there. After plan 001 lands, running the baked suite
requires a reachable Postgres (see that plan's README updates).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Tests run against real PostgreSQL (maintainer mandate) | P1 | M | — (run first) | DONE |
| 002 | Migration-drift check (dedicated CI workflow) | P1 | S | 001 | DONE — on `main` uncommitted. Ships baked `.github/workflows/migrations.yaml` (`uv run manage.py makemigrations --check --dry-run`, DJANGO_ENV=ci, postgres service) AND a matching step in the template's own ci.yaml `bake` job (runs on every bake variant). CI-only per operator: no pytest, no pre-commit, no shell script, no deps, no DEFAULT_AUTO_FIELD. Verified: hooks green, positive exit 0, negative detects drift, full suite 30 passed/100%. Also (same batch): dropped `python` from deploy-check.sh + migrations cmd; trimmed pyproject ALLOWED_HOSTS to `localhost,testserver` (localhost=schemathesis WSGI host, testserver=Django test client; 127.0.0.1 dropped). |
| 003 | Production email sender (DEFAULT_FROM_EMAIL) | P1 | S | — | DONE |
| 004 | Graceful shutdown: drop SIGCONT, SIGTERM drain + rollout rehearsal | P1 | S–M | — | DONE |
| 005 | CI smoke-tests the shipped prod.yaml unpatched | P2 | S–M | 004 (soft) | DONE — merged to `main` (`d79cd17`); CI green, unpatched prod.yaml boots with Compose 5.3.0 |
| 006 | Postgres backup script + restore runbook (compose knob) | P2 | M | — | DONE — committed to `main` (`30703c5`). Live Step 5 verified (dump/restore/prune). NOTE: local prod-stack boot + native script prune require Compose 5.3.0 + GNU head (Linux); this macOS host has 2.40.3/BSD head |
| 007 | Bound container logs + Redis memory | P2 | S | — | DONE — approved (worktree commit `65b3f05`); brought to `main` uncommitted per operator request. Verified: logging on every service across 3 bake variants, live Redis maxmemory 268435456, REDIS_MAXMEMORY gated to redis=compose, baked yamlfmt/markdownlint clean |
| 008 | Test-suite hardening (v1 contract, docs gating, reload leak) | P2 | S | 001 (soft) | DONE — approved (worktree commit `79b068d`); brought to `main` uncommitted per operator request. Verified: full suite 30 passed / 100% cov on fresh bake vs real Postgres, reload-leak fix holds 5/5 randomized. Step-1 fallback taken (v1 schema has zero routes → schemathesis errors on empty schema; plan 009 folds it in). Deviations (all justified, in NOTES): added `protected_docs_urls.py` to `name-tests-test` exclude in `.pre-commit-config.yaml` (out-of-scope but required, mirrors factories.py); test uses `UserFactory.create(is_staff=True)` (plan's `user__is_staff` parametrize doesn't work — is_staff not a factory declaration); helper urlconf includes `admin/` (else staff_member_required's `admin:login` reverse raises) |
| 009 | `use_example_api` knob: notes resource vertical slice | P2 | L | 001, 002, 008 (hard) | DONE — approved (worktree commit `773798d`); on `main` uncommitted per operator. Verified: default bake byte-identical to pre-009 (+uv.lock), knob-OFF has zero notes traces, knob-ON suite 40 passed/100% (incl. schemathesis vs populated v1), migration-drift exit 0. Notes app = model→migration→schemas→`django_auth` CRUD router (ownership-scoped)→factory→11 tests. Deviations (both correct): documented `403` on POST/PUT/DELETE (django_auth CSRF, hit by schemathesis not the test client); `@pytest.mark.django_db(transaction=True)` on the schemathesis test (knob-ON only, byte-identity preserved). FOLLOW-UP if committed+pushed: add "Bake example-api" to branch-protection required checks. |
| 010 | Example periodic task (clear_expired_sessions) + hook matrix fix | P3 | M | 003 (hard), 001 (ordering) | DONE — approved (worktree commit `22b5d7a`, branch `worktree-agent-afbd3408e964f1b0a`); NOT yet on `main`. Reconciled to `60b1aab` first (test tree `tests/unit/core/`→`tests/core/unit/` from commit `9ecd151`). Reviewer-verified: 4-variant render/removal matrix exact (default/worker/wne present, none removed); `CELERY_BEAT_SCHEDULE` present default / absent worker; rendered ruff check+format clean on default/worker/wne; default suite 31 passed & wne 30 passed, both 100% cov vs live Postgres 18.4; eager `.delay()` really deletes expired session (STOP condition clear); actionlint exit 0; `EMAIL_PROVIDER` constant removed (was sole user). NOT independently run: root+baked pre-commit (executor-reported exit 0), live `celery beat` DatabaseScheduler sync (documented-behavior per plan fallback). |
| 011 | ASGI runtime: Gunicorn with Uvicorn worker, ASGI app/tests/docs | P2 | M | 004/005 preferred | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line
reason) | REJECTED (with one-line rationale).

## Dependency notes

- **001 first.** It touches `pyproject.toml` pytest env, both workflow
  files, `dev.yaml`, `deploy-check.sh`, the Dockerfile, and three docs
  files; nearly every later plan re-verifies through the suite it
  changes.
- **002 after 001** (ordering only): both add steps/services to
  `tests.yaml` and the root `bake` job.
- **004 / 005 / 006 / 007 serialize** on `prod.yaml` and the baked
  README Production section — do not implement them concurrently, or the
  drift checks will fire. Recommended order: 004 → 005 → 006 → 007.
- **005 after 004** so CI's unpatched smoke run validates the final stop
  semantics.
- **008 before 009**: 009's endpoints activate the v1 Schemathesis
  coverage 008 introduces, and 009's response-schema requirements are
  written against it.
- **009 needs 002** (its new migration is validated by the drift gate)
  and **001** (its DB-heavy tests should land on Postgres, not be
  rewritten later).
- **010 after 003**: both edit `tasks_test.py`; 010's drift check
  expects 003's sender assertion.
- **003, 006, 007** each touch `.env.example` and/or the baked README —
  serialize with whatever else is in flight on those files.
- **011 after 004/005 preferred**: it keeps Gunicorn as process manager but
  changes the served app from WSGI to ASGI. Let 004/005 stabilize shutdown and
  prod-smoke semantics first, then update the runtime command, schema tests,
  and docs in one pass. Serialize 011 with plans touching `pyproject.toml`,
  `.docker/scripts/gunicorn.sh`, `src/config`, or
  `tests/integration/api/schema_test.py`.

## Findings audited but NOT planned (recorded for the maintainer)

- **No image tagging/registry → no fast rollback** (prod compose builds
  in place; `docker-build.yaml` uses `push: false`). Real, MED effort;
  needs a maintainer call on registry/tagging conventions. Deferred.
- **No metrics surface** (no `/metrics`, no Flower; Sentry-only
  observability). Deliberate scope so far; revisit if operating pain
  appears. The resource bounds in plan 007 are the cheap subset.
- **No CSP/Permissions-Policy on the admin surface** — LOW confidence
  value; admin exposure is already documented as a proxy-level concern.
  Recorded, deferred.
- **Release/versioning workflow** (DIR: `version = "1.0.0"` is static
  but feeds Sentry release tagging, so releases never move without a
  manual bump). Grounded; needs a tooling decision (release-please vs
  tag-driven). Deferred to a maintainer decision.
- **cruft template-update story** — deferred again (recorded twice now);
  the gap between the template and already-baked projects keeps growing.
- **`justfile` task runner** for the long compose command lines — offer
  stands; philosophy headwind (tooling minimalism), maintainer's call.
- **API auth knob (token/JWT)** — partially addressed by plan 009's
  `django_auth` example; a dependency-adding auth knob remains deferred.
- **Shipped `deploy.resources` memory limits** — documented-only in plan
  007; wrong defaults OOM-kill legitimate workloads.

## Settled decisions (carried from the retired plan set — do not re-audit)

- **django-cors-headers**: rejected by the maintainer.
- **API throttling/rate limiting**: rejected by the maintainer.
- **PgBouncer in prod.yaml**: evaluated and rejected (steady-state ~8-9
  connections vs max 100; transaction pooling would force
  `CONN_MAX_AGE=0` and break session semantics). Escalation path:
  psycopg3 native pool first.
- **`.editorconfig`**: rejected, low value next to existing hooks.
- **django-celery-email**: rejected (unmaintained); the project-owned
  `send_email` task is the pattern.
- **`requires-python = ">=3.14,<3.15"` upper bound**: deliberate
  single-minor support window.
- **Unparenthesized `except A, B, C:` in `routes/ready.py`**: valid PEP
  758 on Python 3.14; deliberately kept.
- **`noqa: F821` pattern in settings overlays**: inherent to
  django-split-settings; keep. django-configurations is forbidden.
- **`signals.py` request_id KeyError**: refuted (django-structlog binds
  it before the signals fire).
- **Admin hardening**: proxy-level restriction, documented in the baked
  README; not an app-level plan.
- **Abstract `apps.core` models with no concrete users**: intended
  scaffolding (plan 009 finally consumes them).
- **Redis `noeviction`**: documented tradeoff (broker durability); plan
  007 adds the missing memory *bound*, not a policy change.
- **Traefik + docker-rollout as shipped default**: maintainer decision
  2026-07-04 (reversed the earlier bring-your-own-ingress stance).
- **Sentry boot-required in prod when `use_sentry=yes`**: deliberate.
- **Knob defaults reproduce the historical full-stack output**
  (byte-for-byte) — every new knob must honor this (see plan 009 Step 8).
- **SQLite-in-tests**: the old audit's "real-backend pytest lane
  superseded by the compose smoke test" verdict is **overridden** by an
  explicit 2026-07-05 maintainer mandate — plan 001.

## External review (Codex, 2026-07-05) — disposition

- "Production shutdown is too clever (SIGCONT)" → plan 004 (concurred
  with by this audit independently).
- "CI proves a patched prod file, not the shipped one" → plan 005.
- "plans/ is stale" → resolved by this reset.
- "README should be unapologetic that this is a production template" →
  maintainer explicitly scoped the template as personal ("if other
  people want to use it, fine"); no plan.
- "Vendored .agents/ is a maintenance surface" → acknowledged; the
  refresh mechanism remains deferred direction (recorded in the retired
  set, still open).
